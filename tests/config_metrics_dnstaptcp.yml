verbose: true

collectors:
  dnstap-tcp:
    enable: true
    listen-ip: 0.0.0.0
    listen-port: 6000

generators:
  stdout:
    enable: false

  webserver:
    enable: true
    listen-ip: 0.0.0.0
    listen-port: 8080
    top-max-items: 100
    basic-auth-login: admin
    basic-auth-pwd: changeme

