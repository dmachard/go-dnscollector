trace:
  verbose: false

collectors:
  dnstap:
    enable: true
    listen-ip: 0.0.0.0
    listen-port: 6000

subprocessors:
  filtering:
    log-replies: true
    
loggers:
  stdout:
    enable: true
    mode: json